const placeholder = 'notification text'

const notificationReducer = (notification = placeholder, action) => {
  switch (action.type) {
    default:
      return notification
  }
}

export default notificationReducer
