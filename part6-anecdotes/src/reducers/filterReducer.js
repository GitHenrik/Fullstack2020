const filterAtStart = 'filter text'

//template for course task 6.12
const filterReducer = (filter = filterAtStart, action) => {
  switch (action.type) {
    default:
      return filter
  }
}

export default filterReducer